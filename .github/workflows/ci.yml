name: CI-CD Kubernetes Deployment

on:
push:
branches:
- main
- master

env:
DOCKER_IMAGE: venky254/fitness_tracker

jobs:
build-and-deploy:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout Code
    uses: actions/checkout@v3

  - name: Login to DockerHub
    uses: docker/login-action@v2
    with:
      username: ${{ secrets.DOCKER_USERNAME }}
      password: ${{ secrets.DOCKER_PASSWORD }}

  - name: Build Docker Image
    run: |
      docker build -t $DOCKER_IMAGE:${{ github.sha }} .

  - name: Push Docker Image
    run: |
      docker push $DOCKER_IMAGE:${{ github.sha }}

  - name: Setup kubectl
    uses: azure/setup-kubectl@v3
    with:
      version: latest

  - name: Set Kubernetes Context
    run: |
      echo "${{ secrets.KUBE_CONFIG }}" > kubeconfig
      export KUBECONFIG=$PWD/kubeconfig

  - name: Deploy to Kubernetes
    run: |
      kubectl set image deployment/fitness-deployment fitness-container=$DOCKER_IMAGE:${{ github.sha }}
      kubectl rollout status deployment/fitness-deployment

